# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently **empty** — it contains no source code, build configuration, or documentation yet. This file was generated as a starting point and should be updated as the project takes shape.

## Current State

- No commits exist on the default branch yet.
- No language, framework, or build tooling has been established.
- No CI/CD workflows are configured.

## Guidance for AI Assistants

Until the project has real content, keep the following in mind:

1. **Don't assume structure.** There are no established conventions yet. When adding the first code, ask the user (or infer from their request) which language, framework, and tooling they intend to use rather than defaulting silently.
2. **Establish conventions as you go.** When the first meaningful code lands, update this file with:
   - **Project overview** — what the project does and its high-level architecture
   - **Build & run commands** — how to install dependencies, build, run, and test (including how to run a single test)
   - **Code conventions** — formatting, linting, naming, and directory layout
   - **Development workflow** — branching strategy, commit message style, and any CI checks that must pass
3. **Keep this file current.** Whenever tooling, structure, or workflows change significantly, update the relevant section here so future sessions start with accurate context.

## Maintaining This File

Regenerate or revise this file once the codebase exists — the placeholder guidance above should be replaced with concrete, project-specific commands and architecture notes. Prefer documenting only what is non-obvious from reading the code itself.
